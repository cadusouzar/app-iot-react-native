import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  imagem: {
    width: 200, 
    height: 200,
    marginTop: "18%", 
    marginBottom: "18%"
  }  
});

export default styles

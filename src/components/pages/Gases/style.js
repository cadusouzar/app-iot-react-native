import { StyleSheet } from "react-native";
import { backgroundColor } from "../../../styles/settings/colors";
const styles = StyleSheet.create({
  container:{
    backgroundColor: backgroundColor,
    height: "100%"
  },
  imagem:{
    top: "-7%",
    left: "4%"
  }
});

export default styles